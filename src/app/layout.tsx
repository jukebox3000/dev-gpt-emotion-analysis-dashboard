import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "DevGPT Emotional Dynamics Dashboard",
  description: "Interactive thesis dashboard analyzing emotional dynamics between Developers and ChatGPT using the DevGPT dataset. Features D3.js visualizations with 1,804 conversation turns across 164 threads.",
  keywords: ["DevGPT", "ChatGPT", "emotion analysis", "developer sentiment", "D3.js", "thesis dashboard"],
  authors: [{ name: "DevGPT Research" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
